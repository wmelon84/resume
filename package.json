{
  "name": "resume-deploy",
  "version": "1.0.0",
  "dependencies": {
    "jsonresume-theme-elegant": "^1.13.2",
    "resume-cli": "^1.2.7"
  },
  "scripts": {
    "start": "npm install && npm run build",
    "build": "resume export dist/index.html -t elegant"
  }
}
