{
  "name": "resume-deploy",
  "version": "2.0.0",
  "dependencies": {
    "jsonresume-theme-even": "^0.22.0",
    "resume-cli": "^3.0.8"
  },
  "scripts": {
    "start": "npm install && npm run build",
    "build": "resume export dist/index.html -t jsonresume-theme-even"
  },
  "license": "UNLICENSED",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/wmelon84/resume.git"
  }
}
